# MLOps Unit 1

## Project Objective
This repository demonstrates a foundational MLOps workflow. It includes version control, standard project structuring, model training, evaluation, and reproducibility practices.

## Dataset Used
The project uses the standard **Iris dataset** loaded directly via `scikit-learn`. It features 150 samples of iris flowers with 4 features (sepal length/width, petal length/width) to predict the flower species.

## Steps to Run the Project (Reproducibility)

1. **Clone the repository:**
   ```bash
   git clone <https://github.com/V4IDIK/MLops-unit1>
   cd mlops-unit1
